# Security Policy

## 対象

最新のmainブランチとGitHub Pages公開版を対象にします。

## 設計上の保護

画像処理はブラウザ内で完結し、画像を外部へ送信しません。外部API、解析、広告、外部スクリプト、外部フォントを使用しません。

JPEG、PNG、WebPはファイル名やMIME表示だけでなく、実データの署名と寸法を確認します。容量、画素数、縦横、出力寸法を制限します。

Content Security Policyは外部通信、フレーム、オブジェクト、フォーム送信、ワーカーを拒否します。ユーザー由来の文字列は `textContent` とダウンロード名の正規化を通して扱います。

画像のObject URLと保存用Object URLは使用後に解放します。連続読込では古い非同期処理の結果を破棄します。

## 制約

GitHub Pagesでは任意のHTTPレスポンスヘッダーを設定できません。そのため、HTML内CSPと依存関係を持たない構成を中心に保護しています。

ブラウザの画像デコーダー自体の脆弱性はこのリポジトリだけでは修正できません。OSとブラウザを更新してください。

## 報告時に含める情報

公開Issueへ個人情報や未公開画像を添付しないでください。再現手順、影響、対象ブラウザ、対象ファイル形式、可能なら最小の再現ファイルを記載してください。

危険性が高く公開前の連絡が必要な場合は、GitHubのSecurity advisory機能が利用できるときはそちらを使用してください。
